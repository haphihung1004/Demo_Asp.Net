@model IEnumerable< SachOnline.Models.CHUDE>
<nav class="navbar navbar-inverse">
    <!--navbar-fixed-top-->
    <div class="container">

        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar1" aria-expanded="false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar1">
            <ul class="nav navbar-nav">
                <li><a class="navbar-brand" href="~/SachOnline/">SachOnline</a></li>
                <li class="active"><a href="~/SachOnline/">TRANG CHỦ</a> </li>
                <li><a href="#">GIỚI THIỆU</a> </li>
                <li><a href="#">LIÊN HỆ</a> </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @Html.Action("LoginLogout", "SachOnline")
                @Html.Action("GioHangPartial", "GioHang")


            </ul>
            <form class="navbar-form navbar-right">
            @using (Html.BeginForm("Search", "Search", FormMethod.Get, new { @class = "navbar-form navbar-right", @role = "search" }))
            {
                <div class="form-group">
                <input type="text" class="form-control" placeholder="Tìm kiếm" name="strSearch">
            </div>
            <button type="submit" class="btn btn-default">Tìm</button>
            }
        </form>

        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-->
</nav>